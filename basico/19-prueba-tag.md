# Archivo de prueba para git tag

Este archivo es para practicar:
- git tag
- git tag -a
- git tag -d
- git push --tags

Crea y elimina tags sobre este archivo para experimentar con versiones en Git.
